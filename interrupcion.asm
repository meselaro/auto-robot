
#define F_CPU 8000000UL  //frecuencia de trabajo del ATMEGA88PA
#include "m88PAdef.inc"

　
.ORG	0x0
		RJMP	MAIN
.ORG	0x000B
		RJMP	T1_CM_ISR

MAIN:	
		LDI		R20,HIGH(RAMEND)
		STS		SPH,R20
		LDI		R20,LOW(RAMEND)
		STS		SPL,R20
		SBI		DDRB,5
		LDI		R20,(1<<OCIE1A)
		STS		TIMSK1,R20
		SEI
		LDI		R20,0x00
		STS		TCCR1A,R20
		LDI		R20,0xD
		STS		TCCR1B,R20
		LDI		R20,HIGH(5011)
		STS		OCR1AH,R20
		LDI		R20,LOW(5011)
		STS		OCR1AL,R20
		LDI		R20,0x00
		OUT		DDRC,R20
		LDI		R20,0xFF
		OUT		DDRD,R20
HERE:
		IN		R20,PINC
		OUT		PORTD,R20
		RJMP	HERE

T1_CM_ISR:
		IN		R16,PORTB
		LDI		R17,0x20
		EOR		R16,R17
		OUT		PORTB,R16
		RETI
